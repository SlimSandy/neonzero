#############################################################
#  LAB-9 DOOR CONTROL SYSTEM v2.1
#  Electromagnetic Lock Configuration
#############################################################

[room_31]
lock_type = electromagnetic
status = ENGAGED
power_source = main_grid + battery_backup
failsafe = LOCK_ON_POWER_LOSS
passphrase_required = true
passphrase_hash = 7a3f8b2c9d1e4f06  # SHA-256 truncated

# Override mechanism:
# An override script exists at escape/door_controls/override.sh
# Execute with: ./override.sh <passphrase>
# The passphrase is not stored in plaintext anywhere in
# this system. It was set by Director Sable personally.

[room_31.override]
script_path = escape/door_controls/override.sh
max_attempts = 5
lockout_after_failure = false  # Disabled by Sable
    # "If they somehow get to the override script,
    #  they still need the passphrase. And there's
    #  no way they can assemble it." -- E. Sable
audit_log = /operations/door_audit.log

#############################################################
